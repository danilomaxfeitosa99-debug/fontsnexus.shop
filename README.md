# Fonts Nexus

Site institucional estático em React + TypeScript.

```bash
npm ci
npm run dev
npm run build
```

O workflow `.github/workflows/pages.yml` publica automaticamente no GitHub Pages após o push.
